# keyd configuration
# Lives in the repo as the source of truth, symlinked to /etc/keyd/default.conf
# (keyd only reads /etc/keyd/ — see README for setup).
# Reload after editing with: sudo keyd reload

[ids]

*

[main]

# Caps Lock: Esc on tap, Ctrl on hold
capslock = overload(control, esc)
